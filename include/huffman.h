#include <stdint.h>
#include <stdio.h>

// /** \brief Compress a file using Huffman compression.
// * \param fh_in as FILE*. Input file containing the data to be compressed.
// * \param fh_out as FILE*. Output file containing the compressed data.
// * \return int. Error code (0 if OK).
// */
// int hufmann__compress_file(FILE* fh_in, FILE* fh_out);

// /** \brief Decompress a file that is encoded with Huffman compression.
// * \param fh_in as FILE*. Input file containing the compressed data.
// * \param fh_out as FILE*. Output file containing the decompressed data.
// * \return int. Error code (0 if OK).
// */
// int hufmann__decompress_file(FILE* fh_in, FILE* fh_out);

void hello();